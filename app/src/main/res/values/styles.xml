<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- TODO: Update this style -->
    <style name="DarkTheme" parent="AppTheme">
        <!--    <item name="android:textColor">@color/myText</item>
           <item name="android:windowBackground">@color/myPrimary</item>
           <item name="colorPrimary">@color/myPrimary</item>
           <item name="colorPrimaryDark">@color/myPrimaryDark</item>
           <item name="colorAccent">@color/myAccent</item>
           <item name="buttonStyle">@style/CustomButtonStyle</item>
           <item name="android:tint">@color/myText</item>
           <item name="android:calendarViewStyle">@style/CustomCalendar</item> -->
        <item name="colorPrimary">@color/myAccent</item>
        <item name="colorPrimaryDark">@color/myAccentDark</item>
        <item name="colorAccent">@color/myAccent</item>
    </style>


    <style name="CustomButtonStyle" parent="Widget.AppCompat.Button.Borderless">
        <!-- <item name="android:background">@drawable/custom_button</item> -->
        <item name="android:layout_margin">40sp</item>
        <item name="android:background">@color/myAccent</item>
    </style>

    <style name="CustomCalendar" parent="ThemeOverlay.MaterialComponents.Dark">
        <item name="colorPrimary">@color/myText</item>
        <item name="android:dateTextAppearance">@style/CalenderViewDateCustomText</item>
        <item name="android:weekDayTextAppearance">@style/CalenderViewWeekCustomText</item>

    </style>

    <style name="CalenderViewDateCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/myText</item>
        <item name="android:weekNumberColor">@color/myText</item>
    </style>

    <style name="CalenderViewWeekCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/myText</item>
    </style>

</resources>
